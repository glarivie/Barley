import { StyleSheet } from 'react-native'

import { lightgrey } from '../../styles/shared/variables.styles'

const styles = StyleSheet.create({
  Home: {
    flex: 1,
    backgroundColor: lightgrey,
    paddingTop: 15,
  },
})

export default styles
